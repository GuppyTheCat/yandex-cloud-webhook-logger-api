# Webhook Receiver Cloud Function Dependencies
# Python 3.12

# AWS SDK for Python (YMQ is SQS-compatible)
boto3==1.34.0

# Note: For production, you may want to use Yandex Cloud Lockbox SDK
# However, for simplicity, we'll use environment variables
# yandexcloud==0.270.0

